{
  "name": "hanfani-framework",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The @hanfani/* agent-framework packages.",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "clean": "pnpm -r exec rm -rf dist *.tgz"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20.19"
  }
}
